<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Two Factor Authentication Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки для двухфакторной аутентификации через Google Authenticator
    |
    */

    /*
    |--------------------------------------------------------------------------
    | Issuer Name
    |--------------------------------------------------------------------------
    |
    | Название приложения, которое будет отображаться в Google Authenticator
    | Можно переопределить через переменную окружения TWO_FACTOR_ISSUER
    |
    */
    'issuer' => env('TWO_FACTOR_ISSUER', 'Escobar CRM'),

    /*
    |--------------------------------------------------------------------------
    | QR Code Size
    |--------------------------------------------------------------------------
    |
    | Размер QR-кода в пикселях
    |
    */
    'qr_code_size' => env('TWO_FACTOR_QR_SIZE', 200),

    /*
    |--------------------------------------------------------------------------
    | Window
    |--------------------------------------------------------------------------
    |
    | Количество временных окон для проверки кода (по умолчанию 4)
    | Увеличение этого значения позволяет учитывать рассинхронизацию времени
    |
    */
    'window' => env('TWO_FACTOR_WINDOW', 4),

    /*
    |--------------------------------------------------------------------------
    | Recovery Codes
    |--------------------------------------------------------------------------
    |
    | Включить ли генерацию резервных кодов для восстановления доступа
    |
    */
    'recovery_codes_enabled' => env('TWO_FACTOR_RECOVERY_CODES', true),

    /*
    |--------------------------------------------------------------------------
    | Recovery Codes Count
    |--------------------------------------------------------------------------
    |
    | Количество резервных кодов для генерации
    |
    */
    'recovery_codes_count' => env('TWO_FACTOR_RECOVERY_CODES_COUNT', 8),
];

